// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { FlatbuffersEncryption } from './flatbuffers-encryption.js';


export class UserRecord {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  encryptionCtx: Uint8Array|null = null;
  __init(i:number, bb:flatbuffers.ByteBuffer, encryptionCtx?: Uint8Array):UserRecord {
  this.bb_pos = i;
  this.bb = bb;
  this.encryptionCtx = encryptionCtx || null;
  return this;
}

static getRootAsUserRecord(bb:flatbuffers.ByteBuffer, obj?:UserRecord, encryptionCtx?: Uint8Array):UserRecord {
  return (obj || new UserRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb, encryptionCtx);
}

static getSizePrefixedRootAsUserRecord(bb:flatbuffers.ByteBuffer, obj?:UserRecord, encryptionCtx?: Uint8Array):UserRecord {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UserRecord()).__init(bb.readInt32(bb.position()) + bb.position(), bb, encryptionCtx);
}

userId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

username():string|null
username(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
username(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

passwordHash():string|null
passwordHash(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
passwordHash(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? FlatbuffersEncryption.decryptString(this.bb!.__string(this.bb_pos + offset, optionalEncoding), this.encryptionCtx, 8) : null;
}

privateKey(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

privateKeyLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

privateKeyArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

ssn():string|null
ssn(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
ssn(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? FlatbuffersEncryption.decryptString(this.bb!.__string(this.bb_pos + offset, optionalEncoding), this.encryptionCtx, 12) : null;
}

balance():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? FlatbuffersEncryption.decryptScalar(this.bb!.readFloat64(this.bb_pos + offset), this.encryptionCtx, 14) : 0.0;
}

static startUserRecord(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addUserId(builder:flatbuffers.Builder, userId:bigint) {
  builder.addFieldInt64(0, userId, BigInt('0'));
}

static addUsername(builder:flatbuffers.Builder, usernameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, usernameOffset, 0);
}

static addPasswordHash(builder:flatbuffers.Builder, passwordHashOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, passwordHashOffset, 0);
}

static addPrivateKey(builder:flatbuffers.Builder, privateKeyOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, privateKeyOffset, 0);
}

static createPrivateKeyVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPrivateKeyVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addSsn(builder:flatbuffers.Builder, ssnOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, ssnOffset, 0);
}

static addBalance(builder:flatbuffers.Builder, balance:number) {
  builder.addFieldFloat64(5, balance, 0.0);
}

static endUserRecord(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUserRecord(builder:flatbuffers.Builder, userId:bigint, usernameOffset:flatbuffers.Offset, passwordHashOffset:flatbuffers.Offset, privateKeyOffset:flatbuffers.Offset, ssnOffset:flatbuffers.Offset, balance:number):flatbuffers.Offset {
  UserRecord.startUserRecord(builder);
  UserRecord.addUserId(builder, userId);
  UserRecord.addUsername(builder, usernameOffset);
  UserRecord.addPasswordHash(builder, passwordHashOffset);
  UserRecord.addPrivateKey(builder, privateKeyOffset);
  UserRecord.addSsn(builder, ssnOffset);
  UserRecord.addBalance(builder, balance);
  return UserRecord.endUserRecord(builder);
}
}
